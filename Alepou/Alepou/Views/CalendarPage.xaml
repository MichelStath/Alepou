<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.SfCalendar.XForms;assembly=Syncfusion.SfCalendar.XForms"
             xmlns:viemodels="clr-namespace:Alepou.ViewModels"
             x:Class="Alepou.Views.CalendarPage">
    <ContentPage.BindingContext>
        <viemodels:CalendarViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Orientation="Vertical"
                 VerticalOptions="CenterAndExpand">

        <syncfusion:SfCalendar x:Name="calendar" 
                               InlineViewMode="Agenda"
                               ShowInlineEvents="True"
                               DataSource="{Binding CalendarInlineEvents}"
                               InlineItemTapped="CalendarInlineItemTapped">

            <syncfusion:SfCalendar.MonthViewSettings>
                <syncfusion:MonthViewSettings TodayBorderColor="#ff0000"/>
            </syncfusion:SfCalendar.MonthViewSettings>
        </syncfusion:SfCalendar>

    </StackLayout>
</ContentPage>